return {
    notifs = require(... .. '.notifs'),
    -- errors = require(... .. '.errors'),
    layouts = require(... .. '.layouts'),
    sloppy = require(... .. '.sloppy'),
    titlebars = require(... .. '.titlebars'),
    bar = require(... .. '.bar')
    -- wallpaper = require(... .. '.client')
}
