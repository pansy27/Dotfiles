[global]
    monitor = 0

    follow = mouse

    width = (0, 240)

    height = 600

    origin = top-right

    notification_limit = 0

    progress_bar = true

    indicate_hidden = yes

    separator_height = 2

    text_icon_padding = 3

    frame_width = 3

    separator_color = frame

    sort = yes

    idle_threshold = 20

    font = Poppins Regular 8
    line_height = 4

    padding = 09
    horizontal_padding = 9

    markup = full

    format = "<span><b>%s</b>%p</span>\n%b"

    alignment = left

    vertical_alignment = center

    show_age_threshold = 60

    ellipsize = middle

    ignore_newline = no

    stack_duplicates = true

    hide_duplicate_count = false

    show_indicators = yes

    icon_position = left

    min_icon_size = 0

    max_icon_size = 32

    icon_path = $HOME/.local/share/icons/Macaron/status/:$HOME/.local/share/icons/Macaron/devices/:$HOME/.local/share/icons/Macaron/apps/

    sticky_history = yes

    history_length = 10

    dmenu = rofi -dmenu -p dunst:

    browser = /usr/bin/xdg-open

    always_run_script = true

    title = Dunst

    class = Dunst

    corner_radius = 3

    ignore_dbusclose = true

    layer = top

    mouse_left_click = close_current
    mouse_middle_click = do_action, context
    mouse_right_click = close_all

[urgency_low]
   background = "#242118"
    foreground = "#fcdddd"
    frame_color = "#FBC1C0"
    timeout = 5

[urgency_normal]
    background = "#f2e9de"
    foreground = "#464587"
    frame_color = "#E08BA4"
    timeout = 10

[urgency_critical]
   background = "#242118"
    foreground = "#fcdddd"
    frame_color = "#e35259"
    timeout = 15
    # Icon for notifications with critical urgency, uncomment to enable
    #new_icon = /path/to/icon

