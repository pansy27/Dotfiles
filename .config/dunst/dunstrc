[global]
    origin = top-right
    offset = 24x24

    background = "#302e31"
    foreground = "#ffffff"
    highlight = "#1488ff"
    padding = 16

    frame_width = 0
    frame_color = "#25252540"

    progress_bar_height = 16
    progress_bar_frame_width = 0

    idle_threshold = 60

    # Text
    font = LINE Seed JP_TTF 10
    format = "<span text_transform='uppercase'>%s</span>\n%b"

    # Icons
    icon_position = left
    min_icon_size = 64
    max_icon_size = 64

    # History
    sticky_history = no
    history_length = 20

    # Mouse
    mouse_left_click = close_current
    mouse_middle_click = do_action, close_current
    mouse_right_click = close_all

[shortcuts]
    close = ctrl+space
    close_all = ctrl+shift+space
    history = ctrl+grave
    context = ctrl+shift+o

[urgency_low]
    foreground = "#999999"
    timeout = 5

[urgency_normal]
    timeout = 5

[urgency_critical]
    timeout = 30
